<template>
  <div>
    <!-- Используем NuxtLayout для применения лейаута (например, default.vue) -->
    <!-- Лейаут будет содержать навигацию, футер и т.д. -->
    <NuxtLayout>
      <!-- NuxtPage - это место, куда будут рендериться компоненты страниц из папки pages/ -->
      <NuxtPage />
    </NuxtLayout>
  </div>
</template>

<script setup lang="ts">
// Здесь можно добавить глобальную логику, если она нужна для всего приложения,
// например, установку мета-тегов по умолчанию или инициализацию чего-либо.
// Но чаще всего основная логика находится в страницах, компонентах,
// лейаутах, плагинах и сторах Pinia.

// Пример установки заголовка по умолчанию
// useHead({
//   titleTemplate: (titleChunk) => {
//     return titleChunk ? `${titleChunk} - Debts Analyzer` : 'Debts Analyzer';
//   }
// })
</script>

<style>
/* Здесь можно разместить САМЫЕ ГЛОБАЛЬНЫЕ стили, которые применяются абсолютно везде. */
/* Например, сброс стилей или базовые стили для body/html */
/* Однако, большинство глобальных стилей лучше размещать в файлах CSS/SCSS */
/* и подключать их через nuxt.config.ts (как мы сделали с Bootstrap/main.scss) */

/* body {
  font-family: sans-serif;
  margin: 0;
  background-color: #f8f9fa;
} */
</style>